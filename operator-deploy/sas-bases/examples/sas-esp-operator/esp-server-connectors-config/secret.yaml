---
apiVersion: v1
kind: Secret
metadata:
  name: sas-esp-config-connectors-config
stringData:
  connectors.config: |
    [timer_pub]
    basetime=2024-01-01 00:00:00
    interval=2
    timeformat=%Y-%m-%d %H:%M:%S
    type=pub
    unit=second
    [fs_sub]
    dateformat=%Y-%m-%d %H:%M:%S
    fsname=events.json
    fstype=json
    snapshot=false
    type=sub
